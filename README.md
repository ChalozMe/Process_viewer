# Process viewer (like fake btop)
academical work

```bash
Requires Linux, python
```

this GUI allows copy all actual proces runnig in the pc, add new ones(fakes), rename and set duration of them.
when is charged with process you can selec diferente proceses solvers algorithims like *SJF, RR, FCFS* and can use "N" procesor cores.

when you lauch the simulation you can see all the proces and how they be cuted to procesed.
this is not 100% true couse there is no consideration thing like priority process or deamons.
